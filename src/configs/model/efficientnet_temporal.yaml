# @package _global_
# EfficientNetV2-S frame encoder + temporal transformer — Open World Track (pretrained)
model:
  name: efficientnet_temporal
  pretrained: true
  num_classes: ${num_classes}
  embed_dim: 512
  num_heads: 8
  num_layers: 4
  dropout: 0.1
  use_checkpoint: true
